org.gradle.jvmargs=-Xmx2048m
android.useAndroidX=true
android.nonTransitiveRClass=true
# Gradle 9.1.0+ / AGP 9.0.1+ (see build.gradle and gradle/wrapper) run fine
# under a JDK 25 daemon — no JAVA_HOME override needed anymore. App bytecode
# itself still targets Java 17 (see app/build.gradle compileOptions).
