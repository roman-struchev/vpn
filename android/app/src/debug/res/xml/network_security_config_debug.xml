<?xml version="1.0" encoding="utf-8"?>
<!--
  Debug-build-only (see src/debug/AndroidManifest.xml) — the main manifest
  keeps usesCleartextTraffic="false" for release. Needed so ApiFlowTest (and
  any local dev pointing -PapiBaseUrl at a plain-http server) can actually
  reach it: 10.0.2.2 is the AOSP emulator's alias for the host loopback,
  where the real local server (see root README.md) runs over http, not https.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
