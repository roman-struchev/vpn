{
  "name": "vpn-e2e",
  "version": "1.0.0",
  "private": true,
  "description": "Playwright end-to-end tests exercising the assembled server+web stack (integration, not component-level).",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui",
    "install-browsers": "playwright install chromium --with-deps"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0"
  }
}
